<h1>New Restaurant Page(search)</h1>
<p>Need to check for valid data in Cuisine.</p>
<p>Zip is optional, maybe autofill it from user info if its there?</p>
<p>Maybe prompt for a zip before showing the results?</p>
<%= render partial: 'new', locals: { restaurant: @restaurant } %>
<%= link_to "Restaurants home", restaurants_path %>
