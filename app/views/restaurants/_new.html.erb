<%= form_for restaurant, :url => {:action => "search"} do |f|%>
 <%= f.label "Cuisine" %><br>
 <%= f.text_field :cuisine %><br>
 <%= f.label "Zip" %><br>
 <%= f.text_field :zip %>
 <%= f.submit 'Search' %>
<% end %>
<%# need to check for missing data above %>
