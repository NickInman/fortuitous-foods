<% if !alert.nil? %>
<% binding.pry %>
<% alert.each do |warn| %>
	 <strong><%= warn %></strong><br>
  <% end %><br>
<% end %>
<%= render partial: 'new', locals: { user: @user } %>
<%= link_to "Back",root_path %><br>
