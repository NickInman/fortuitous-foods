  <%= image_tag(src = r.image_url, size: "200") %>
  <%= r.name %>
  <%= r.cuisine %>
  <%= r.review_count %>
  <%= r.rating %>
  <%= r.price %>
  <%= r.city %>
  <%= r.zip %>
  <%= link_to r.name, r.url %><br><br>
